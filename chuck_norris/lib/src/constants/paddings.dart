import 'package:flutter/material.dart';

class CustomPaddings {
  static const smallPaddingHorizontal = EdgeInsets.symmetric(horizontal: 8);
  static const mediumPaddingHorizontal = EdgeInsets.symmetric(horizontal: 20);
  static const largePaddingHorizontal = EdgeInsets.symmetric(horizontal: 30);

  static const smallPaddingVertical = EdgeInsets.symmetric(vertical: 8);
  static const mediumPaddingVertical = EdgeInsets.symmetric(vertical: 20);
  static const largePaddingVertical = EdgeInsets.symmetric(vertical: 30);

  static const smallPaddingAll = EdgeInsets.all(8);
  static const mediumPaddingAll = EdgeInsets.all(12);
  static const largePaddingAll = EdgeInsets.all(30);

  static const listPadding = EdgeInsets.only(bottom: 300, top: 50);
  static const listItemPadding = EdgeInsets.only(bottom: 5);
  static const favoriteScreenPadding =
      EdgeInsets.only(bottom: 150, left: 18, right: 18);
}
